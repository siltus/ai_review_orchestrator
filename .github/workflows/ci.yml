name: ci

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: |
          python -m pip install --upgrade pip setuptools
          python -m pip install -e ".[dev]"
      - name: ruff (lint)
        run: python -m ruff check src tests
      - name: ruff (format check)
        run: python -m ruff format --check src tests
      - name: pip-audit (supply-chain)
        run: python -m pip_audit --skip-editable
      - name: pytest
        # --cov-fail-under=90 enforces the AGENTS.md non-negotiable coverage
        # baseline at the CI level; [tool.coverage.report] fail_under = 90
        # in pyproject.toml enforces the same floor for `coverage report`.
        run: python -m pytest --cov=aidor --cov-report=term-missing --cov-fail-under=90
